local __util = require 'larian.util'

-- 基础等级判断函数
function IsLevelHigherThen(number)
	if GetModifier(context.Source.Level) >= number then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 检查是否拥有特定武功
function HasKungFuPassive(passiveName)
	if HasPassive(context.Source, passiveName) then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 检查内力点数是否足够
function HasEnoughKiPoints(requiredPoints)
	local currentKi = GetActionResourceValueCap(context.Source, "KiPoint", 0)
	if currentKi >= requiredPoints then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 检查武功组合效果
function HasKungFuCombo(passive1, passive2)
	if HasPassive(context.Source, passive1) and HasPassive(context.Source, passive2) then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 计算武功熟练度加成
function GetKungFuMasteryBonus()
	local level = GetModifier(context.Source.Level)
	local bonus = math.floor(level / 4) + 1
	return bonus
end

-- 检查是否在战斗中
function IsInCombat()
	if context.Source.Combat ~= nil then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 检查生命值百分比
function GetHealthPercentage()
	local currentHP = GetModifier(context.Source.HP)
	local maxHP = GetModifier(context.Source.MaxHP)
	if maxHP > 0 then
		return (currentHP / maxHP) * 100
	end
	return 0
end

-- 检查是否处于低血量状态
function IsLowHealth(threshold)
	local healthPercent = GetHealthPercentage()
	if healthPercent <= (threshold or 25) then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end

-- 检查武功类型数量
function GetKungFuTypeCount()
	local count = 0
	local kungfuTypes = {
		"Passive_KFC_HuangDiNeiJing", "Passive_KFC_ShenZhaoJing", -- 医药
		"Passive_KFC_JinGangBuHuaiShenGong", "Passive_KFC_DaJinGangShenLi", -- 硬功
		"Passive_KFC_MingMuGong", "Passive_KFC_ShenJiBaiBian", "Passive_KFC_ZuoYouShouHuBoShu", -- 软功
		"Passive_KFC_TiYunZong", "Passive_KFC_YiWeiDuJiang", "Passive_KFC_LingBoWeiBu", "Passive_KFC_FeiXuQingYanGong", -- 轻功
		"Passive_KFC_YiJinJing", "Passive_KFC_XiSuiJing", "Passive_KFC_TianCanGong", "Passive_KFC_BuSiYinFa", "Passive_KFC_TaiXuanJing", -- 内功
		"Passive_KFC_WuLeiTianXinJue", -- 外功
		"Passive_KFC_TaiJiQuan", "Passive_KFC_DouZhuanXingYi", "Passive_KFC_QianKunDaNuoYi" -- 拆招
	}
	
	for _, passive in ipairs(kungfuTypes) do
		if HasPassive(context.Source, passive) then
			count = count + 1
		end
	end
	
	return count
end

-- 检查是否达到武功大师级别
function IsKungFuMaster()
	local level = GetModifier(context.Source.Level)
	local kungfuCount = GetKungFuTypeCount()
	if level >= 10 and kungfuCount >= 5 then
		return ConditionResult(true)
	end
	return ConditionResult(false)
end